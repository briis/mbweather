default_config:
cloud:

mbweather:
  host: !secret mbweather_ip
  username: !secret mbweather_user
  password: !secret mbweather_pass

binary_sensor:
  - platform: mbweather

sensor:
  - platform: mbweather

weather:
  - platform: mbweather
    api_key: 753ba3e433efaca0a83c299828153bb4
    mode: daily
    name: Vindinge

logger:
  default: error
  logs:
    custom_components.mbweather: debug
